{
  "name": "workgrid-memory",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @workgrid/desktop dev",
    "build": "pnpm --filter @workgrid/desktop build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "tauri": "pnpm --filter @workgrid/desktop tauri",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
